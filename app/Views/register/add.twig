{% extends "base.twig" %}

{% block body %}

    <h1>Register add</h1>

{#    {{data.arrErrors|raw}}#}
    {{data.form_open|raw}}
    {{data.form_id|raw}}
    {{data.label_email|raw}}
    {{data.form_email|raw}}
    {{data.label_password|raw}}
    {{data.form_password|raw}}
    {{data.form_submit|raw}}
    {{data.form_close|raw}}

{% endblock %}