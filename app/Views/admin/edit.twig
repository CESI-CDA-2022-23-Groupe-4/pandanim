{% extends "base.twig" %}

{% block title %} Admin - Edit User {% endblock %}

{% block body %}

    <h1>Edit</h1>

    {% for err in data.arrErrors %}
        {{ err }}
        <br>
    {% endfor %}

    {{data.form_open|raw}}
    <div>{{data.form_id|raw}}</div>
    <div>{{data.label_username|raw}}</div>
    <div class="mb-3">{{data.form_username|raw}}</div>
    <div>{{data.label_firstname|raw}}</div>
    <div class="mb-3">{{data.form_firstname|raw}}</div>
    <div>{{data.label_lastname|raw}}</div>
    <div class="mb-3">{{data.form_lastname|raw}}</div>
    <div>{{data.label_email|raw}}</div>
    <div class="mb-3">{{data.form_email|raw}}</div>
    <div>{{data.label_password|raw}}</div>
    <div class="mb-3">{{data.form_password|raw}}</div>
    <div>{{data.label_roles|raw}}</div>
    <div class="mb-3">{{data.form_roles|raw}}</div>
    <div class="mb-3">{{data.form_submit|raw}}</div>
    <div class="mb-3">{{data.form_close|raw}}</div>

{% endblock %}